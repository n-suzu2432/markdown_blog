# 学習メモ

このブログを作るときにわからなくて調べた内容をまとめたメモ。

---

## 1. `@tailwindcss/typography` とは何か

### 何が問題だったのか

MarkdownをHTMLに変換すると、こんな素のHTMLが出てくる。

```html
<h1>タイトル</h1>
<p>本文です</p>
<code>コード</code>
```

Tailwind CSS は「自分でクラスを書かない限り、何もスタイルをあてない」設計なので、
このHTMLは**全部同じ見た目になってしまう**。

- `<h1>` も `<p>` も同じ文字サイズ
- 見出しと本文の間に余白がない
- リストの「・」が消える
- `<blockquote>` が普通のテキストと区別できない

### どう解決するか

`@tailwindcss/typography` をインストールして、`prose` クラスを1つ書くだけで全部いい感じになる。

```html
<div class="prose">
  <!-- この中のHTMLが自動で整形される -->
</div>
```

### `prose` クラスで何が変わるか

| 要素 | 変化 |
|------|------|
| `<h1>` 〜 `<h3>` | 大きさが変わり、下に余白がつく |
| `<p>` | 行間が広がって読みやすくなる |
| `<ul>` `<ol>` | 「・」や番号が復活する |
| `<blockquote>` | 左に線がついて引用っぽくなる |
| `<code>` | 背景色がついてコードとわかる |
| `<table>` | 罫線・縞模様がついて見やすくなる |

### 一言まとめ

> Markdownで書いた記事を、**本や雑誌みたいにきれいに整形してくれる魔法のクラス**。
> これがないと、どんなに丁寧に記事を書いても画面では全部同じ見た目になってしまう。

---

## 2. `src/` ディレクトリとは何か

### Next.js のファイルの置き場所は2パターンある

**パターンA：`src/` なし（シンプル）**

```
プロジェクト/
├── app/          ← ここにページファイルを置く
├── public/
└── package.json
```

**パターンB：`src/` あり（整理整頓型）**

```
プロジェクト/
├── src/          ← 「ソースコード専用の箱」
│   └── app/      ← その中にページファイルを置く
├── public/
└── package.json
```

どちらでも動く。ただの好みの問題。

### なぜ問題になるのか

設計図（要件定義）と実際のプロジェクトで置き場所がズレていると、
AIにファイルを作ってもらったときに場所がバラバラになる。

- 要件定義を読んだAI → `src/lib/posts.ts` に作る
- 実際のプロジェクト → `src/` がないので場所がズレる

### このプロジェクトでの決定

**`src/` なし**に統一。  
理由：プロジェクトの初期状態が `src/` なしだったので変更ゼロで済むから。

```
プロジェクト/
├── app/        ← ルート直下
├── lib/        ← ルート直下
├── components/ ← ルート直下
└── public/
```

---

## 3. Lighthouse Performance の確認方法

### Lighthouse とは

Google が作った「ウェブサイトの通知表」。表示速度・効率などを100点満点で採点してくれる。
Performance が **90以上** がこのプロジェクトの達成基準。

### Chrome DevTools で計測する手順

1. ブラウザで計測したいページを開く（例：`http://localhost:3000/posts`）
2. `F12` キーで DevTools を開く
3. 上のタブから **「Lighthouse」** を選ぶ（見当たらない場合は `>>` をクリック）
4. 「Categories」の **Performance** にチェックが入っていることを確認
5. **「Analyze page load」** ボタンをクリック
6. 1〜2分待つとスコアが表示される

### 注意点

| 注意 | 理由 |
|------|------|
| **シークレットモードで計測する**（`Ctrl+Shift+N`） | ブラウザ拡張機能の影響でスコアが下がるのを防ぐ |
| **本番ビルドで計測する**（`npm run build && npm start`） | 開発サーバー（`npm run dev`）はデバッグ処理が多くスコアが低く出やすい |

### 一言まとめ

> `npm run build && npm start` で起動 → シークレットモードで開く → DevToolsのLighthouseタブで計測、が**正確なスコアを出す正しい手順**。
